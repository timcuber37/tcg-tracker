# Vite exposes only VITE_-prefixed vars to the browser.
# The anon key is the PUBLIC key (Settings → API → "anon public") — safe to ship,
# NOT the service_role key.
VITE_SUPABASE_URL=https://yytalcwwkzbfjbsgjeww.supabase.co
VITE_SUPABASE_ANON_KEY=<paste-anon-public-key-here>
